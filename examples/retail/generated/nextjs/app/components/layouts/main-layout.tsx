import GlobalHeader from "@/app/components/gadgets/global-header";
import GlobalSidebar from "@/app/components/gadgets/global-sidebar";
import GlobalFooter from "@/app/components/gadgets/global-footer";

/**
 * PageLayout: 17595b62-fef1-4b22-9c25-16736c772567 (Main Layout)
 * regions: header / sidebar / footer / main
 * assignments:
 *   header  → 68709449 (グローバルヘッダ)
 *   sidebar → c1cff7da (ナビゲーションサイドバー)
 *   footer  → f7daa764 (グローバルフッタ)
 *   main    → page content slot
 * cssFramework: tailwind
 *
 * Generated by Harmony /generate-code (RFC #1021 pl-7).
 */
export default function MainLayout({
  children,
  storeName,
  userName,
  copyright,
  version,
}: {
  children: React.ReactNode;
  storeName?: string;
  userName?: string;
  copyright?: string;
  version?: string;
}) {
  return (
    <div className="flex min-h-screen flex-col">
      <header className="flex items-center justify-between bg-blue-900 px-4 py-3 text-white shadow">
        <GlobalHeader storeName={storeName} userName={userName} />
      </header>

      <div className="flex flex-1">
        <aside className="w-64 border-r bg-gray-100 p-4">
          <GlobalSidebar />
        </aside>

        <main className="flex-1 p-6">{children}</main>
      </div>

      <footer className="mt-auto border-t bg-gray-50 py-4 text-center">
        <GlobalFooter copyright={copyright} version={version} />
      </footer>
    </div>
  );
}
