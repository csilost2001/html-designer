import { NextRequest, NextResponse } from "next/server";

/**
 * Route Handler: POST /api/gadgets/global-header/act-logout
 *
 * Gadget: 68709449 (グローバルヘッダ)
 * ProcessFlow: 60e08c25 (ヘッダーガジェット処理) — act-logout
 *
 * 本来は session 破棄 + login へ redirect。本デモでは / に redirect する stub。
 *
 * Generated by Harmony /generate-code (RFC #1021 pl-7).
 */
export async function POST(request: NextRequest) {
  // TODO: session.destroy() を invoke する認証連携実装
  return NextResponse.redirect(new URL("/", request.url));
}
